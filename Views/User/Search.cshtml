@model List<CRUD_application_2.Models.User>

<h2>Search Results</h2>

<table>
	<tr>
		<th>Name</th>
		<th>Email</th>
	</tr>
	@foreach (var user in Model)
	{
		<tr>
			<td>@user.Name</td>
			<td>@user.Email</td>
		</tr>
	}
</table>
